{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Parameters for Speed Scale Estimator Module",
  "type": "object",
  "definitions": {
    "speed_scale_estimator": {
      "type": "object",
      "properties": {
        "update_interval": {
          "type": "number",
          "description": "Timer interval for estimation updates in seconds. Unit is **[s]**.",
          "minimum": 0.0,
          "default": 0.3
        },
        "max_pose_lag": {
          "type": "number",
          "description": "Maximum allowed time difference between consecutive poses used for pose velocity. Samples exceeding this value reset the previous pose. Unit is **[s]**.",
          "minimum": 0.0,
          "default": 0.5
        },
        "max_stamp_lag": {
          "type": "number",
          "description": "Maximum allowed absolute timestamp difference between the current pose and the nearest IMU or velocity report sample. Unit is **[s]**.",
          "minimum": 0.0,
          "default": 0.2
        },
        "sensor_buffer_duration": {
          "type": "number",
          "description": "Duration of the rolling buffer for IMU and velocity report messages used for timestamp synchronization. Unit is **[s]**.",
          "minimum": 0.0,
          "default": 1.0
        },
        "initial_speed_scale_factor": {
          "type": "number",
          "description": "Initial speed scale factor value used before estimation begins.",
          "minimum": 0.0,
          "default": 1.0114008010492628
        },
        "initial_speed_scale_factor_covariance": {
          "type": "number",
          "description": "Initial covariance of the speed scale factor state (P).",
          "minimum": 0.0,
          "default": 0.01
        },
        "process_noise_covariance": {
          "type": "number",
          "description": "Process noise covariance of the speed scale factor state (Q).",
          "minimum": 0.0,
          "default": 1.0e-6
        },
        "measurement_noise_covariance": {
          "type": "number",
          "description": "Measurement noise covariance used in the Kalman filter update (R).",
          "minimum": 0.0,
          "default": 200.0
        },
        "max_angular_velocity": {
          "type": "number",
          "description": "Maximum allowed IMU angular velocity in rad/s. Samples exceeding this value are filtered out to reject obvious curves and turns. Default 0.0105 rad/s (~0.6 deg/s) is chosen around the practical IMU bias/noise floor; tune per vehicle and operation (e.g. with IMU bias cancellation) if needed. Unit is **[rad/s]**.",
          "minimum": 0.0,
          "default": 0.0105
        },
        "max_speed": {
          "type": "number",
          "description": "Maximum allowed pose velocity in m/s. Samples exceeding this value are filtered out to avoid extreme speeds that may cause inaccurate estimation. Unit is **[m/s]**.",
          "minimum": 0.0,
          "default": 17.0
        },
        "min_speed": {
          "type": "number",
          "description": "Minimum required pose velocity in m/s. Samples below this value are filtered out to ensure sufficient motion for reliable pose-differentiation-based estimation. Unit is **[m/s]**.",
          "minimum": 0.0,
          "default": 6.0
        }
      },
      "required": [
        "update_interval",
        "max_pose_lag",
        "max_stamp_lag",
        "sensor_buffer_duration",
        "initial_speed_scale_factor",
        "initial_speed_scale_factor_covariance",
        "process_noise_covariance",
        "measurement_noise_covariance",
        "max_angular_velocity",
        "max_speed",
        "min_speed"
      ],
      "additionalProperties": false
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/speed_scale_estimator"
        }
      },
      "required": ["ros__parameters"]
    }
  },
  "required": ["/**"]
}
