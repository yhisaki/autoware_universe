{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Parameters for Image Transport Decompressor",
  "type": "object",
  "definitions": {
    "autoware_image_transport_decompressor": {
      "type": "object",
      "properties": {
        "encoding": {
          "type": "string",
          "description": "The encoding to publish. Use `rgb8` or `bgr8`. Any other value publishes the encoding the sender named, which may not match the payload; see Assumptions / Known limits.",
          "default": "default"
        }
      },
      "required": ["encoding"]
    }
  },
  "properties": {
    "/**": {
      "type": "object",
      "properties": {
        "ros__parameters": {
          "$ref": "#/definitions/autoware_image_transport_decompressor"
        }
      },
      "required": ["ros__parameters"]
    }
  },
  "required": ["/**"]
}
